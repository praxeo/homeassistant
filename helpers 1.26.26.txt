

{% set helper_domains = ['input_boolean', 'input_number', 'input_datetime', 'input_text', 'input_select', 'input_button', 'counter', 'timer', 'schedule'] %}
{% for state in states | sort(attribute='entity_id') %}
{% if state.domain in helper_domains %}
{{ state.entity_id }}
  name: {{ state.name }}
  state: {{ state.state }}
  {% for attr, value in state.attributes.items() if attr not in ['friendly_name', 'icon'] %}
  {{ attr }}: {{ value }}
  {% endfor %}
{% endif %}
{% endfor %}
input_boolean.basement_work_mode
  name: Basement Work Mode
  state: off
  
  editable: True
  



input_boolean.garage_work_mode
  name: Garage Work Mode
  state: off
  
  editable: True
  



input_boolean.mail_likely_delivered_today
  name: Mail (likely) delivered today
  state: off
  
  editable: True
  



input_boolean.mail_likely_retrieved_today
  name: Mail (likely) retrieved today
  state: off
  
  editable: True
  



input_number.front_porch_previous_brightness
  name: Front Porch Previous Brightness
  state: 0.0
  
  initial: None
  
  editable: True
  
  min: 0.0
  
  max: 100.0
  
  step: 1.0
  
  mode: slider
  
  unit_of_measurement: %
Result type: string

This template listens for all state changed events.